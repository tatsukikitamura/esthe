<h1>ようこそ！</h1>

<div class="user-info">
  <h2>ユーザー情報</h2>
  
  <% if current_user.profile_image.attached? %>
    <div class="user-profile-image">
      <%= image_tag current_user.profile_image, alt: current_user.name, class: "user-main-image" %>
    </div>
  <% end %>
  
  <p><strong>名前:</strong> <%= current_user.name || "未設定" %></p>
  <p><strong>メールアドレス:</strong> <%= current_user.email %></p>
  
  <div class="profile-actions">
    <%= link_to "プロフィール編集", edit_user_path(current_user), class: "btn btn-info" %>
  </div>
</div>

